export default function Tutor() {
  // AITutor (always rendered in AuthenticatedStudyTools) detects this route
  // and auto-expands to cover the full screen. This page is the background canvas.
  return <div className="min-h-screen bg-slate-950" />;
}
